---
sidebar_position: 2
---

import useBaseUrl from '@docusaurus/useBaseUrl';

# MongoDB Atlas

If we switch to the `Data Services` tab in MongoDB Atlas and we browse our collections, we'll that a new database has been created. This database has the name we set when we enabled Development Mode while configuring Device Sync.

[![Documents inside the collection](/img/4-checking-data/collections.png)](/img/4-checking-data/collections.png)